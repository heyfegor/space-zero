# dependencies
node_modules/

# next.js
.next/
out/
next-env.d.ts

# env / secrets — ignore every .env variant EXCEPT the committed templates
.env
.env.*
!.env.example
!.env.*.example

# credentials / keys (AWS, Supabase, Duffel, etc.) — never commit
.aws/
aws.json
credentials
credentials.*
*.pem
*.key
*.p12
*.pfx
secrets.json
service-account*.json

# local scratch / test payloads
bedrock-test.json

# build / cache
*.tsbuildinfo

# os / editor
.DS_Store
Thumbs.db
